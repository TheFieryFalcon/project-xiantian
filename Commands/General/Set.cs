using ProjectXiantian.Definitions;

namespace ProjectXiantian.Commands.General {
    partial class GeneralCommands {
        public static void Set(GameContext context, char[] flags, string[] parameters, bool debug) {
            // TODO: Implement this + test Use (should be simple, just parse as consequent statement)
        }
    }
}